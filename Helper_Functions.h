#ifndef HELPER_FUNCTIONS_H_INCLUDED
#define HELPER_FUNCTIONS_H_INCLUDED
#include<iostream>

using namespace std;

unsigned short get_size(string data); //this function returns the size of string

long long power(int number, int power);

int str2int(string number);

float str2float(string numebr);

#endif // HELPER_FUNCTIONS_H_INCLUDED
